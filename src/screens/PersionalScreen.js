import React from 'react';
import { Text, View } from 'react-native';
import styled from 'styled-components/native';

function PersionalScreen({ navigation }) {
    return (
        <View>
            <Text>Personal Screen</Text>
        </View>
    );
}

export default PersionalScreen;
